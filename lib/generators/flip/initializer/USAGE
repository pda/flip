Description:
    Generates the Feature initializer class for Flip.

Example:
    rails generate flip:initializer

    This will create:
        app/config/initializers/feature.rb (class Feature < ActiveRecord::Base)
